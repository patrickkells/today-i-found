# Keep this file secret-free. Configure the token in the scheduled task's secret store.
CURATOR_TOKEN=
# Optional protected Worker endpoint, for example: https://feedback.example/v1/editions
REGISTER_EDITION_URL=
# Optional protected Worker endpoint, for example: https://feedback.example/v1/feedback/summary?days=90
FEEDBACK_SUMMARY_URL=
