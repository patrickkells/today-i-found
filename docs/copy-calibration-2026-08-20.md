# Copy calibration: August 20, 2026

The five published stories provided the baseline for `broad-tech-copy-v2`. Each selected version names the concrete change, adds decision-useful information in the summary, and stays inside the verified evidence.

The plain-language and AI-tell checks adapt the compact [Cursor PStack Unslop skill](https://github.com/cursor/plugins/blob/main/pstack/skills/unslop/SKILL.md). Its opinion and personality guidance is intentionally excluded because this briefing is factual, not commentary.

## Gemini 3.7 Flash

Baseline: **Gemini 3.7 Flash is generally available for coding and agents**

Selected: **Google makes Gemini 3.7 Flash generally available**

Selected summary: The stable Gemini API endpoint includes improvements for software engineering, web development, and agent workflows.

The selected version names the actor and removes the vague audience framing from the headline.

## Qwen 3.8 27B

Baseline: **Qwen 3.8 27B lands on Workers AI with vision and function calling**

Selected: **Cloudflare adds Qwen 3.8 27B to Workers AI**

Selected summary: The hosted model accepts text and images, supports reasoning and function calls, and has a 262,144-token context window.

The selected version replaces `lands` with the concrete platform action and moves capabilities into the summary.

## Fish Audio

Baseline: **Vercel AI Gateway adds Fish Audio speech and transcription models**

Selected: **Vercel adds four Fish Audio models to AI Gateway**

Selected summary: The models support speech generation and transcription. Free model IDs stop serving after September 18, while standard IDs use paid billing.

The selected version gives the model count and keeps the offer boundary because it affects whether a reader should try the integration.

## Vercel for Slack

Baseline: **Vercel Agent enters Slack with approval-gated production actions**

Selected: **Vercel releases its Slack agent as a public beta**

Selected summary: Pro and Enterprise teams can investigate deployment issues from Slack and approve production changes. Only a limited number of simple requests are free before usage-based billing.

The selected version removes the promotional `enters` construction and states eligibility, capability, and billing limits.

## Instagram adapter

Baseline: **Chat SDK adds an Instagram adapter for DM bots**

Selected: **Vercel adds Instagram messaging to Chat SDK**

Selected summary: The adapter handles direct messages, media, quick replies, reactions, and story replies without a separate Instagram transport integration.

The selected version names the publisher and replaces a raw feature list with the integration consequence.
